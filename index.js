class Formatter {
  //add static methods here

  static capitalize(str){
    return str[0].toUpperCase() + str.slice(1);
  }
  static sanitize (str){
    srt1 = str.split('-').split(`${range(1,9)}`)
  }
  static titleize (str){

  }

}